1 группа. checkTriangle()


    Негативные сценарии:
        1.1. Вызов метода со значением стороны                  a = 0, a = -0.1;            ожидается       false,    triangle.getMessage.equals("a<=0");
                                                                b = 0, b = -0,1;            ожидается       false,    triangle.getMessage.equals("b<=0");
                                                                c = 0, c = -0,1;            ожидается       false,    triangle.getMessage.equals("c<=0");

        1.2. Вызов метода со значениями сторон, при котором     a + b = c, a + b < c;       ожидается       false,    triangle.getMessage.equals("a+b<=0");
                                                                a + c = b, a + c < b;       ожидается       false,    triangle.getMessage.equals("a+c<=0");
                                                                b + c = a, b + c < a;       ожидается       false,    triangle.getMessage.equals("b+c<=0");

        1.3. Вызов метода со значениями сторон, при котором возможно переполнение                         fixme (если переполнение возможно)


    Позитивные сценарии:
        1.4. Вызов метода со значением стороны                  a = 0.1;                    ожидается       true,    triangle.getMessage.equals("");
                                                                b = 0,1;                    ожидается       true,    triangle.getMessage.equals("");
                                                                c = 0,1;                    ожидается       true,    triangle.getMessage.equals("");

        1.5. Вызов метода со значениями сторон, при котором     a + b > c;                  ожидается       true,    triangle.getMessage.equals("");
                                                                a + c > b;                  ожидается       true,    triangle.getMessage.equals("");
                                                                b + c > a;                  ожидается       true,    triangle.getMessage.equals("");


2 группа. detectTriangle()


        2.1. Вызов метода со значением сторон    a = b, a = c, b = c;                       ожидается ;   fixme (уточнить побитовые)
                                                 a = b, a = c, b != c;                      ожидается ;   fixme (уточнить побитовые)
                                                 a != b, a != c, b != c;                    ожидается ;   fixme (уточнить побитовые)
                                                (a*a+b*b == c*c)...                         ожидается ;   fixme (уточнить побитовые)

        2.2. Вызов метода со значением сторон из п.2.1 на граничных значениях с шагом 0,1 ( a - b = 0.1)  fixme (уточнить шаг)

        2.3. Вызов метода с данными из п.1.1 и п.1.2 должны вызывать исключение, поскольку фигура не является треугольником.

        2.4. Вызов метода со значениями сторон, при котором возможно переполнение                         fixme (если переполнение возможно)


3 группа. getSquare()

        3.1 Вызов метода с данными из п.1.1 и п.1.2 должны вызывать исключение, поскольку фигура не является треугольником.
        3.2 Вызов метода со значениями сторон, при котором возможно переполнение                          fixme (если переполнение возможно)

